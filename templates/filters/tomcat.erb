
       if [type] == "tomcat" {
         multiline {
           pattern => "^%{TOM_TIME}"
           negate => true
           what => "previous"
         }
         grok {
           match => { "message" => "%{TOMCATLINE}" }
         }
       }

